% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_expression.R
\name{gene_expression}
\alias{gene_expression}
\title{Un-normalized count data for mouse retina RNA-seq samples}
\usage{
gene_expression(format = c("long", "wide"), only_expressed_genes = FALSE)
}
\arguments{
\item{format}{A string indicating the format of the returned object. Must be
one of: \code{"long"} (default): a tibble with one row per gene × sample combination.
\code{"wide"}: a numeric matrix of counts with genes as rows and sample
IDs as columns --- suitable for downstream differential expression
analysis.}

\item{only_expressed_genes}{Logical. If \code{TRUE}, filters out genes that have
zero counts in all samples. Default is \code{FALSE}.}
}
\value{
\describe{
\item{Long format:}{A tibble with columns:
\describe{
\item{\code{sample_id}}{Character. Unique sample identifier matching those in \code{\link[=samples]{samples()}}.}
\item{\code{ensembl_gen_id}}{Character. Ensembl gene ID (e.g., \code{ENSMUSG00000000001}).}
\item{\code{gene_symbol}}{Gene symbol (e.g., \code{Gnai3}), provided for convenience.}
\item{\code{counts}}{Integer. Raw expression counts for that gene in that sample.}
}}
\item{Wide format:}{A numeric matrix. Rows correspond to genes (named by
\code{ensembl_gen_id}), columns to samples (column names are \code{sample_id}s).
Expression values are raw un-normalized counts.}
}
}
\description{
\code{\link[=gene_expression]{gene_expression()}} returns gene-level raw expression counts from the
spaceflight and ground-based experiment involving mouse retina samples.
}
\details{
The dataset comprises 35 samples and expression counts for ~57,000 genes
(Ensembl IDs).
}
\examples{
# Un-normalized counts in long format
gene_expression()

# Un-normalized counts in wide format (matrix)
gene_expression("wide")

}
\seealso{
\code{\link[=samples]{samples()}}
}
